name: 🐧 Linux (Flatpak)

on:
  push:
  pull_request:
  schedule:
    - cron: '0 0 * * 2'

jobs:
  flatpak:
    name: 📦 Build Flatpak
    runs-on: ubuntu-latest
    container:
      image: bilelmoussaoui/flatpak-github-actions:gnome-47
      options: --privileged
    env:
      CARGO_HOME: /root/.cargo
      RUSTUP_HOME: /root/.rustup

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install Rust
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain 1.96.1 --no-modify-path
          echo "/root/.cargo/bin" >> "$GITHUB_PATH"
          /root/.cargo/bin/rustc --version

      - name: Vendor Cargo dependencies
        run: cargo vendor

      - name: Install Rust SDK extension
        run: flatpak install -y org.freedesktop.Sdk.Extension.rust-stable//24.08

      - name: Build Flatpak
        run: |
          flatpak-builder --repo=repo build-dir flatpak/com.github.samfic.szyszka.yml --force-clean
